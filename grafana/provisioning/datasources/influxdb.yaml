apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: plantwise
      defaultBucket: sensors
    secureJsonData:
      token: plantwise-dev-token
    isDefault: true
    editable: false
